namespace Accountant.Web.Resources;

/// Marker type for product-area string localization (App + Administration).
/// Resource files live next to this class as `ProductResource.bg.resx` etc.
/// Auth strings continue to come from `Braikov.Identity.Core.Resources.SharedResource`
/// (configured in Program.cs).
public sealed class ProductResource
{
}
